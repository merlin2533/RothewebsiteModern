# Block all web access to this directory.
# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2 (legacy)
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
